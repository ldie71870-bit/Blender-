# Mesh 引导高斯实现状态

## 已实现并验证

- 以 `1.2.7-chunked-background-render` 为基线升级到 `1.2.8`。
- 默认折叠 UI、三种工作模式和一键 modal 阶段机。
- 场景保存、输出目录、Mesh、内存预算和渲染器预检。
- dependency graph 最终 Mesh/实例提取、世界变换、逆转置法线、UV 与 ID 元数据。
- 面积加权表面采样、小物体权重和基础曲率权重。
- 打包/外部/生成图像像素读取与双线性 UV 采样。
- 扁平高斯位置、旋转、尺度、透明度、SH0 和 Mesh 锚点。
- NPZ、标准 3DGS PLY、GLB 和材质/Mesh 元数据。
- Blender/OpenCV 坐标集中转换、相机直接导出和训练/验证划分。
- RGB、Depth、Normal、Object ID、Triangle ID 真值与数量一致性验证。
- 原子任务状态、日志、取消、恢复、中间文件策略。
- 外部训练进程、配置、日志、进度读取和取消。
- 初始 PLY 加载与输出目录入口。

## 已接入但依赖外部条件

- “一键生成并训练”会自动定位同工作区训练器和独立虚拟环境，但仅在 `environment_report.json` 成功后启用。
- 最终训练 PLY 的质量和 checkpoint 恢复由外部训练器负责。
- 场景 RGB 保持当前 Cycles/EEVEE 渲染设置；V-Ray 自动化不稳定时明确阻止流程。

## 部分实现

- 自动相机筛选：已实现相机集合隔离、姿态去重、数量上限和确定性划分；尚未实现基于 Triangle 可见集合的贪心表面覆盖评分。
- 自适应采样：已使用面积、三角法线变化、小物体权重和 UV 顶点颜色跨度；尚不包含程序材质烘焙后的高频纹理与轮廓误差反馈。
- 复杂材质：能识别程序化 Base Color 并明确回退，尚未自动建立临时对象/UV 执行 PBR 烘焙。
- 任务恢复：数据生成阶段按对象/阶段恢复，旧渲染器按帧/分块恢复；外部训练进程重启后不自动接管旧 PID。

## 未实现

- 完整的 GPU Triangle/Object 可见性栅格化和 98% 表面覆盖求解。
- 程序化材质 Base Color/Alpha/Normal/Roughness/Metallic/Emission 自动烘焙。
- 完整 UDIM 跨 tile 采样。
- V-Ray 特殊节点网络自动解析与自动渲染。
- 毛发/粒子到 Mesh 的通用转换。

这些项目没有以空文件、假数据或固定成功状态伪装为完成。输出元数据会对不可用通道明确标记。

## 已知问题

- 合并所有采样分块并写最终 NPZ 时会出现一次与目标点数成正比的内存峰值；预检按字段预算拦截明显超限配置。
- 大型 GLB 导出和单张 Blender 渲染仍是同步操作；流水线按阶段/相机切片保持其余时间可响应。
- 材质槽 ID 以每个对象的槽索引为准；跨对象全局材质身份应读取 `material_metadata.json` 和对象实例记录。
- 训练器必须自行验证其支持本版本 NPZ 字段和 logit/log-scale 约定。

