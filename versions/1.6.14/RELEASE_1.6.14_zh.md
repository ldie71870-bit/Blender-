# 1.6.14：覆盖颜色独立于相机代理

修复轻量代理切换恢复 Camera.display_size 时，覆盖缓存被误判失效的问题。相机更新现在比较实际光学签名；显示大小和代理元数据不触发失效，位置、朝向、焦距、传感器、偏移和裁剪等实际变化仍要求重新检测。

Blender 5.1.1 回归通过：连续开关代理并改变原生线框大小，覆盖状态保留；修改焦距正确失效；遮挡、笔刷、补拍与两种模式测试仍通过。旧版本已经失效的覆盖结果需要安装修复版后重新检测一次。
